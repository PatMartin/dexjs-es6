/**
 *
 * This class will enforce constraints on Csv input into the components.
 * by performing type validation via a specification shorthand language.
 *
 **/
export default class Specification {
  /**
   *
   * Construct a new specification.
   *
   * @param {string} name - The name of the specification.
   * @property {string} name - The name of the specification.
   */
  constructor(name) {
    this.name = name;
  }
}